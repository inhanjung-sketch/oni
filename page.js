# 온이의 별 쌓기

책 일곱 권을 읽고 500자 이상 감상평을 쓰면 별이 채워지고, 일곱 개를 다 모으면 뽑기를 할 수 있는 독서 기록 앱입니다.

## Vercel에 배포하는 방법

### 방법 1: GitHub을 거쳐서 (추천)

1. GitHub에 새 저장소를 만들고 이 폴더 전체를 업로드합니다.
2. https://vercel.com 에 가입(또는 로그인)한 뒤 "Add New Project"를 누릅니다.
3. 방금 만든 GitHub 저장소를 선택합니다.
4. 별다른 설정 없이 "Deploy"를 누르면 끝입니다. Next.js 프로젝트는 Vercel이 자동으로 인식합니다.
5. 배포가 끝나면 `https://프로젝트이름.vercel.app` 같은 주소가 생깁니다.

### 방법 2: Vercel CLI로 바로 배포 (GitHub 없이)

터미널(맥은 터미널 앱, 윈도우는 명령 프롬프트)을 열고 이 폴더로 이동한 뒤:

```
npm install -g vercel
vercel
```

안내에 따라 로그인하고 질문에 엔터만 누르면 됩니다. 끝나면 배포 주소가 출력됩니다.

## 휴대폰에서 앱처럼 쓰기

1. 배포된 주소를 휴대폰 브라우저(사파리 또는 크롬)로 엽니다.
2. 사파리는 공유 버튼 → "홈 화면에 추가", 크롬은 메뉴(점 세 개) → "홈 화면에 추가"를 누릅니다.
3. 홈 화면에 아이콘이 생기고, 탭하면 주소창 없이 앱처럼 열립니다.

## 기록 저장에 대해

기록(읽은 책, 감상평, 별 개수)은 각 휴대폰의 브라우저 안에만 저장됩니다(localStorage). 서버에 올라가지 않고, 브라우저 데이터를 지우거나 다른 기기로 바꾸면 기록이 사라집니다. 여러 기기에서 같은 기록을 보거나 데이터를 영구적으로 백업하려면 별도의 데이터베이스 연동이 필요한데, 필요하시면 추가로 작업해드릴 수 있습니다.

## 뽑기 상품 비율 수정하기

`app/page.js` 파일 상단의 `PRIZES` 배열에서 `weight` 숫자를 바꾸면 당첨 비율이 바뀝니다. 숫자가 클수록 더 자주 나옵니다.

## 부정행위 감지 기준 수정하기

`app/page.js`의 `PROFANITY`, `KEYBOARD_MASH` 배열에 단어를 추가하거나 빼면 감지 기준을 조정할 수 있습니다.
